name: sample action
on: push
jobs:
  remote-hello-world:
    runs-on: ubuntu-latest
    steps:
      - name: Use remote action
        uses: Maxitoth/My-first-public-Action@v1
        with:
          who-to-greet: 'maxi'
